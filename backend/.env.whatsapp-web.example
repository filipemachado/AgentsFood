# Configuração para WhatsApp Web (apenas para desenvolvimento/teste)
# Adicione estas variáveis ao seu arquivo .env para habilitar o WhatsApp Web

# Habilitar WhatsApp Web (false por padrão)
WHATSAPP_WEB_ENABLED=true

# ID do establishment (obtido do banco de dados)
# Para obter: Faça login como admin e acesse GET /api/establishment
WHATSAPP_WEB_ESTABLISHMENT_ID="your-establishment-id-from-database"

# Pasta para armazenar sessão do WhatsApp (opcional)
WHATSAPP_WEB_SESSION_PATH="./whatsapp-session"

# Reconexão automática (true recomendado)
WHATSAPP_WEB_AUTO_RECONNECT=true

# Timeout para QR Code em milissegundos (60 segundos)
WHATSAPP_WEB_QR_TIMEOUT=60000

# ===== INSTRUÇÕES DE USO =====
# 
# 1. Copie as variáveis acima para seu arquivo .env
# 
# 2. Configure WHATSAPP_WEB_ESTABLISHMENT_ID:
#    - Faça login na API: POST /api/auth/login
#    - Com o token, acesse: GET /api/establishment
#    - Copie o "id" do establishment
# 
# 3. Inicie o servidor: npm run start:dev
# 
# 4. Nos logs do servidor, aparecerá um QR Code
#    - Abra WhatsApp no seu celular
#    - Vá em "Dispositivos Conectados" > "Conectar um dispositivo"
#    - Escaneie o QR Code mostrado no terminal
# 
# 5. Teste a conexão via API:
#    - GET /api/whatsapp-web/status (ver status da conexão)
#    - POST /api/whatsapp-web/send-test (enviar mensagem teste)
# 
# 6. Mensagens recebidas no WhatsApp serão processadas automaticamente
#    pelo agente IA e as respostas enviadas de volta
# 
# IMPORTANTE: 
# - WhatsApp Web só funciona com um dispositivo conectado por vez
# - Se você já usa WhatsApp Web no navegador, desconecte primeiro
# - Mantenha o servidor rodando para manter a conexão ativa